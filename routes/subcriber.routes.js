const authentication = require('../controllers/authen.controllers')
const authorMdw = require('../middlewares/authorUser.mdw')
module.exports = function (app){
    /*
    app.get('/editprofile', authentication.getEditProfileView);
    router.put('/password', authentication.changePassword);
    router.put('/',  authentication.updateProfile);*/
}